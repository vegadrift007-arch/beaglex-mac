import SwiftUI

struct LaunchItemsView: View {
    var body: some View {
        PlaceholderView(title: "Launch Items", phase: "Phase 7", icon: "powerplug")
            .navigationTitle("Launch Items")
    }
}
